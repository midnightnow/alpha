# THE KNIGHT-RESIDUE COUPLING THEOREM

## 1. The 3-Smooth Hierarchy ($2^a 3^b$)
The system constants $\{24, 64, 96\}$ are members of the 3-smooth family ($2^a 3^b$). This property ensures that the Discrete Fourier Transform (DFT) and Fast Fourier Transform (FFT) are maximally efficient, allowing the "Prime Wave" to be sampled without computational aliasing.

## 2. The Cartesian Residue Square ($S \times S$)
- **The Sieve**: Modulo 24 produces the 8-element residue group $S = (\mathbb{Z}/24\mathbb{Z})^\times = \{1, 5, 7, 11, 13, 17, 19, 23\}$.
- **The Board**: The $8 \times 8$ chessboard is mapped to the Cartesian product $S \times S$ via a bijection $\phi: \{0,\dots,7\} \to S$. Every square represents a unique pair of prime-admissible residues.
- **The Knight**: The knight move (Euclidean length $\sqrt{5}$) acts as a permutation on this $S \times S$ lattice.

## 3. The 96-Cycle Theorem
**Theorem**: On a toroidal $8 \times 8$ lattice, the Knight Operator ($K$) induces a permutation of the 64 squares whose cycle lengths are divisors of **96**. 

Because 96 is the least common multiple of the Spatial Period (8) and the Spectral Alignment (24), the Knight’s trajectory is the only path that performs a **Complete State Traversal** of the prime-residue lattice. To move the Knight is to cycle through the residues of the infinite prime wave.

## 4. The Hades Gap ($12.37\%$)
The transition from the theoretical alignment of **96** to the active nodes of **93** is the **$12.37\%$ Hades Gap** (93/96 ≈ 0.968, but the volumetric ratio $\sqrt{42}/\sqrt{51} \approx 0.907$ provides the physical "play"). This gap is the aperture where the static math becomes a living song.

---
**Status:** BEDROCK SECURED.
**Registry:** Volume 42.0 | Shell 93 | Cycle 96
